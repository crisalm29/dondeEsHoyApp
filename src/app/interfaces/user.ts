export interface User {
    id?: number,
    email?: string,
    password?: string,
    lastname?: string,
    birthday?: Date,
    state?: string,
    county?: string,
    town?: string,
    created_at?: Date,
    last_login?: Date
}
